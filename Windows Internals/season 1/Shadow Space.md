
![[Pasted image 20260112031631.png]]

[[Calling Convention]]

زمانی که ما میخواهیم یک فانکشنی یا بهتر بگم calle رو صدا بزنیم باید 32byte یا DWORD به caller فظا اختصاص بدیم در انالیز اسمبلی ها هم دیدیم که نوع type DWPRD بودش

اما دقیقا چه اتفاقی می افتد

```asm
call
cmp eax,ebx ----> push to stack ----> return address 
jump ----> call inttuction
```

به این خاطر میره داخل Stack چون باید ادرس رو داشته باشیم تا درقدم بعدی بپریم به مقدار ادرس اون فانکشنه که call شده  


![[Pasted image 20260112033509.png]]

![[Pasted image 20260112033833.png]]



![[Pasted image 20260112034358.png]]

![[Pasted image 20260112034543.png]]

call 
هم در اصل داره یه دونه push انجام میده 


پس به صورت کلی shadow space یه مکانیزم  برای debug هستش و اگر برنامه مون بیشتر از 4 ارگومان داشته باشه میره بعد از sahdow sapce میشینه
زمانی به کار ما میاد که بخواهیم پارامتر هامون رو debug کنیم یعنی داخل RSP+28h


![[Pasted image 20260112035518.png]]

وقتی داریم push میکنیم در اصل داریم 8 تا از RSP کم میکنیم و میریزیم دیتا رو میریزه تو Stack 

RSP-8

دستور pop هم میاد اخرین مقدار ایتم Stack رو برمیداره RSP رو میریزه به اونجایی که ما بهش میگیم 


---

برای اینکه بیایم و در معماری 32bit کد اسمبلی بزنیم باید بیایم و در قدم اول با استفاده از استراکچر 

```c
__asm(push 5);
```

اما در 64bit به این صورت نیست و به این شکل میریم جلو 

![[Pasted image 20260112040518.png]]

بر روی source file کلیک کرده و یه فایل asm میسازیم 

![[Pasted image 20260112040607.png]]

بر روی پروژمون کلیک کرده و این گزینه رو میزنیم 

![[Pasted image 20260112040634.png]]

تیک رو میزنیم 

![[Pasted image 20260112040703.png]]

بر روی خوده فایل asm کلیلک کرده و وارد بخش propertis میشیم و رو این گزینه میزنیم و تمام 

حالا دیگه میتونیم کد assembly  بزنیم 

```asm
.code

hello proc 

hello endp

end
```

در زبان Assembly باید همیشه یک code و end داشته باشیم و باید همیشه از طریق یک فانکشن بریم اون ور یعنی وارد زبان C بشیم  

![[Pasted image 20260112041019.png]]

به این صورت تابع مون رو میسازیم که external هست 

حالا در بخش Assembly اسم تابع یعنی hello رو صدا میکنیم و میگیم proc که به معنای prociger هست 
و به endp که به معنای end prosiger هست خاتمه میدیم

---

پس به صورت کلی ما در معماری 64bit یک مکانیزمی داریم تحت عنوان shadow spase که برای debug مورد استفاده قرار میگیره 
و عملکردش به این صورت هست که اگر یک فانکشنی بخواد توسط caller صدا زده بشه باید 32 بایت فظا توسط صدا زننده به calle پاس داده میشود 

اما چه استفاده از اون میکنه ؟ مثلا ما میایم دستور call در Aeembly رو میزنیم میاد ادرس دستور بعد از call میریزه داخل stack  که بعدا بدونه کجا برگرده که این همون Return Address هستش که 8 byte داره و یه 32 byte هم shadow space هستش که چهارتا 8 بایت
اما چرا چهارتا 8 بایت چون ارگومان های ما از طریق Rgistry های CPU منتقل میشن 

interger
	- RDX
	- RCX
	- R8
	- R9
float 
	xmm0-3

وقتی این چهارتا از طرق registry  منتقل میشن و اگر ما بخواهیم دیباگ کنیم یا ارگومان های Stack رو نگاه کنیم به مشکل میخوریم  برای همین  ویندوز اومده یه مکانیزمی رو گذاشته که در بحث دیباگ ما بتونیم اون value های ریجستری رو ببینیم 


---

## سلسله مراتبی که فانکشن ها  همدیگرو صدا میزننده رو میگن call stack 

---

## مسیری که ماکروسافت لیست blacklist driver نگه میداره


```python
c:\windows\apppatch\drvmain.sdb
```


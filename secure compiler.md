



---

## 🎯 هدف شما چیست؟

برای انتخاب روش مناسب باید بدانیم هدف شما دقیقاً کدام است:

| هدف | بهترین ابزار / کامپایلر |
|------|--------------------------|
| جلوگیری از مهندسی معکوس یا سرقت کد | ✅ LLVM Obfuscator یا VMProtect / Themida |
| رمزنگاری واقعی فایل اجرایی برای اجرا فقط توسط خودتان | ✅ Intel SGX / ARM TrustZone (محیط‌های سخت‌افزار امن) |
| فشرده‌سازی و پنهان‌سازی ساده (سبک‌تر ولی کم‌ امنیت‌تر) | ✅ UPX یا Enigma Protector |

بیایید پرکاربردترین سناریو را — مثلاً "محافظت و رمزنگاری خروجی باینری" — کامل توضیح دهیم.

---

## 🧩 ۱. استفاده از **Obfuscator-LLVM (نسخه امنیتی LLVM/Clang)**

### 🔸 کاربرد:
کد ++C/C را به باینری تبدیل می‌کند ولی در طول کامپایل، منطق کد را رمزنگاری یا گیج‌کننده می‌کند.

### 🔸 مراحل نصب و استفاده (در لینوکس یا ویندوز):

1. **دانلود نسخه‌ی سفارشی LLVM با obfuscator**
   - [Obfuscator-LLVM GitHub](https://github.com/obfuscator-llvm/obfuscator)
   - این نسخه در واقع همان clang است با پلاگین امنیتی.

2. **کامپایل کد با فعال‌سازی گزینه‌های رمزنگاری:**  
   مثلا:
   ```bash
   clang -mllvm -sub -mllvm -bcf -mllvm -fla source.c -o secure.exe
   ```
   توضیح گزینه‌ها:
   - `-sub`: جایگزینی عبارات منطقی با زیرعبارات معادل ولی پیچیده.
   - `-bcf`: جریان کنترل دروغین (bogus control flow).
   - `-fla`: پیچیدگی در جریان منطقی برنامه.

3. **نتیجه:**
   خروجی باینری مثل exec یا exe کاملاً عملکرد دارد اما مهندسی معکوس آن تقریباً غیرممکن است.

---

## 🧩 ۲. استفاده از **VMProtect** (در ویندوز)

### 🔸 کاربرد:
اجرایی نهایی را پس از کامپایل رمزنگاری و مجازی‌سازی می‌کند.

### 🔸 روش استفاده:
1. کد خود را با کامپایلر معمولی کامپایل کن:
   ```bash
   gcc mycode.c -o app.exe
   ```

2. باز کن در **VMProtect** (نرم‌افزار گرافیکی):
   - بخش‌های حساس برنامه را انتخاب کن (مثلاً توابع خاص).
   - حالت محافظتی را انتخاب کن (Encrypt / Virtualize / Mutate).
   - خروجی نهایی بگیر (مثلاً `app_protected.exe`).

3. این فایل نهایی حتی اگر دی‌کامپایل شود، شبه‌کد بی‌معنا برمی‌گرداند.

---

## 🧩 ۳. استفاده از **UPX** برای فشرده‌سازی رمزنگاری‌مانند

اگر امنیت بالا نمی‌خواهی ولی می‌خواهی اجرایی کم‌حجم و تاحدی محافظت‌شده باشد:

```bash
gcc main.c -o app
upx app
```

این باعث می‌شود باینری فشرده و تاحدی از دید دی‌کامپایلرها دشوار شود.  
(البته با `upx -d app` قابل بازگردانی است، پس امنیت بالایی ندارد.)

---

## 🧩 ۴. برای امنیت سطح سخت‌افزار (رمزنگاری واقعی)

اگر بخواهی کد فقط روی دستگاه خاص اجرا شود (مثلاً رمزنگاری داخل CPU):

- در پلتفرم **Intel SGX**:
  1. نصب Intel SGX SDK
  2. ساخت Enclave پروژه
  3. کامپایل با `sgx_edger8r` و `gcc -DENCLAVE`
  4. خروجی .so یا .dll، فقط در پردازنده پشتیبان اجرا می‌شود

این سطح کاملاً سخت‌افزاری است و برای پروژه‌های امنیتی پیشرفته توصیه می‌شود.

---

## 🔹 پیشنهاد نهایی برای شروع

اگر تازه شروع کردی و می‌خواهی **خروجی کدت در برابر مهندسی معکوس محافظت شود** بدون نیاز به سخت‌افزار خاص:

👉 استفاده از **Obfuscator-LLVM** بهترین نقطه شروع است:
- رایگان  
- کراس‌پلتفرم  
- سطح امنیت بالا  
- بدون تغییر در کد منبع اصلی

---


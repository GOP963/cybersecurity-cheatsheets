

- `objdump`
- `objcopy`
- `radare2`

زبان Assembly یک زبان سطح پایین هست یعنی به شدت به سخت افزار وابسته هستش به همین خاطر کدی که مینویسین و به اون سخت افزاری که دارین و  نوشتین وابسته هستش به همین خاطر معروف هست به Non-portable یعنی چی ؟ یعنی به راحتی قابل حمل نیست 

![[Pasted image 20260311200056.png]]

بعد از توضیحاتی که تا اینجا دادیم، تعجب‌آور نیست که یک دستور ساده در زبان C++ بتواند به چندین دستور در زبان اسمبلی تبدیل شود (و در نتیجه چندین کد ماشین تولید کند)، همان‌طور که در مثال زیر دیده می‌شود.


زبان‌های برنامه‌نویسی سطح بالا زیادی وجود دارند که زندگی توسعه‌دهندگان را بسیار آسان‌تر می‌کنند. برای برخی از برنامه‌نویسان، این زبان‌ها حتی فرایند توسعه نرم‌افزار را به یک هنر و فعالیتی لذت‌بخش تبدیل می‌کنند. ساخت برنامه‌ها با زبان‌های سطح بالا بسیار ساده‌تر است و علاوه بر آن، اشکال‌زدایی (Debug) و نگهداری (Maintenance) آن‌ها نیز راحت‌تر انجام می‌شود.

سؤال: پس چرا باید یک زبان سطح پایین یا به‌اصطلاح قدیمی یاد بگیریم؟

پاسخ: زیرا هنگام انجام مهندسی معکوس (Reverse Engineering)، دقیقاً با همین سطح از کد سروکار خواهید داشت.


### 3.1.2 زبان اسمبلی – چرا مورد نیاز است؟

برای مدت طولانی، از زبان اسمبلی برای نوشتن کدهای بسیار بهینه و سریع استفاده می‌شد، زیرا کنترل مستقیم‌تری روی سخت‌افزار و عملکرد برنامه فراهم می‌کرد.

اما امروزه با وجود کامپایلرهای مدرن که دارای بهینه‌سازهای هوشمند هستند، نیاز به استفاده گسترده از زبان‌های سطح پایین کاهش یافته است.

### هدف از یادگیری زبان اسمبلی

یادگیری اسمبلی می‌تواند با اهداف زیر انجام شود:

1. نوشتن کد برای وظایف خاص مرتبط با سخت‌افزار، مانند درایورهای سیستم‌عامل (که خارج از محدوده این دوره است).
    
2. مهندسی معکوس کد اجرایی یک برنامه برای:
    
    - پیدا کردن آسیب‌پذیری‌ها و ساخت اکسپلویت
    - درک عملکرد یک فایل اجرایی (در تحلیل بدافزار، دقیقاً به همین مورد نیاز داریم)

### اسمبلی در فرایند دیباگ

هنگام دیباگ کردن نرم‌افزار، ما فرایند در حال اجرا را هدف قرار می‌دهیم و وارد کد اجرایی آن (کد ماشین) می‌شویم.

خواندن این کد به صورت دستورهای اسمبلی بسیار آسان‌تر از خواندن دنباله‌های طولانی از اعداد هگزادسیمال (Byte-code) است.


:::writing
از آنجا که زبان اسمبلی در واقع نمایشی قابل‌فهم برای انسان از کد ماشین است، اولین گام در مسیر یادگیری ما، درک معماری یک ماشین (CPU) می‌باشد.

در یک نگاه سطح بالا، پردازنده (CPU) از اجزای زیر تشکیل شده است:

• **واحد کنترل (Control Unit – CU):** وظیفه مدیریت اجرای دستورها و جریان داده بین سایر اجزای پردازنده را بر عهده دارد.

---

### نمای کلی معماری CPU (ادامه)

• **رجیسترها (Registers):** حافظه‌های بسیار کوچک و سریعی هستند که برای استفاده مستقیم توسط دستورهای ماشین به کار می‌روند. تقریباً بیشتر دستورهای پردازنده در یکی از عملوندهای خود به یک رجیستر اشاره می‌کنند.

• **واحد حساب و منطق (Arithmetic and Logic Unit – ALU):** مسئول انجام عملیات‌های حسابی و منطقی (عملیات بیت‌به‌بیت) روی مقادیر صحیح (Integer) است.

• **واحد محاسبات اعشاری (Floating‑Point Unit – FPU):** برای انجام عملیات‌های ریاضی روی اعداد اعشاری (Float) استفاده می‌شود.

---

### معماری ساده در مقابل CPUهای مدرن

معماری‌ای که توضیح داده شد یک مدل ساده از CPU است. در پردازنده‌های مدرن، واحدهای بیشتری وجود دارند و همچنین حافظه‌های داخلی بیشتری مانند **Cache Memory** اضافه شده‌اند. برخی از CPUها حتی یک **واحد پردازش گرافیکی (GPU)** داخلی نیز دارند.

تمام این اجزای اضافی که در این دوره بررسی نمی‌شوند، مفاهیم اصلی مطرح‌شده را تغییر نمی‌دهند؛ بلکه فقط راه‌های بیشتری برای دستکاری کد در اختیار ما قرار می‌دهند یا به درک بهتر **عملکرد پردازنده و تکنیک‌های بهینه‌سازی** کمک می‌کنند.
:::


![[Pasted image 20260311201227.png]]



:::writing
### واحد کنترل (Control Unit – CU)

واحد کنترل در CPU وظایف زیر را انجام می‌دهد:

• دریافت (Fetch) کدهای ماشین از حافظه اصلی (Memory)  
• رمزگشایی (Decode) کدها و تبدیل آن‌ها به دستورهای قابل اجرا  
• بارگذاری و ذخیره داده‌ها در رجیسترها (Registers)  
• دستور دادن به سایر واحدهای پردازنده برای انجام عملیات‌های مشخص  
• تکرار مداوم این چرخه برای اجرای برنامه‌ها

به طور ساده، این فرایند همان **چرخه Fetch → Decode → Execute** است که به صورت پیوسته در CPU انجام می‌شود.

اجزای اصلی در این معماری شامل موارد زیر هستند:

- CU (واحد کنترل)  
- Registers (رجیسترها)  
- ALU (واحد حساب و منطق)  
- FPU (واحد محاسبات اعشاری)  
- Main Memory (RAM)

همه این اجزا در کنار هم، سیستم پردازشی CPU را تشکیل می‌دهند.

---

### رجیسترها (Registers)

در این دوره معماری **Intel® 64** بررسی می‌شود که همان معماری **۶۴ بیتی** و رایج امروزی است. این معماری توسط **AMD64** نیز استفاده می‌شود و با نام‌های **x64** یا **x86_64** نیز شناخته می‌شود.

در پردازنده‌های Intel® 64 چهار نوع اصلی رجیستر وجود دارد:

#### 1. رجیسترهای عمومی (General Purpose Registers)

- شامل **۱۶ رجیستر**  
- هر کدام **۶۴ بیت** اندازه دارند  
- به دلیل کاربردهای متنوع، «عمومی» نامیده می‌شوند

کاربردهای آن‌ها شامل:

• عملوندها در عملیات‌های حسابی و منطقی  
• استفاده در محاسبات آدرس حافظه  
• نگهداری اشاره‌گرها به مکان‌های حافظه

---

#### 2. رجیسترهای سگمنت (Segment Registers)

- شامل **۶ رجیستر ۶۴ بیتی**

این رجیسترها برای **مدیریت تقسیم‌بندی حافظه (Memory Segmentation)** استفاده می‌شوند؛ مانند:

- فضای کد (Code Segment)  
- فضای داده (Data Segment)

---

#### 3. رجیستر RFLAGS

- یک رجیستر **۶۴ بیتی**

این رجیستر شامل **پرچم‌ها (Flags)** است که هر بیت آن معنای خاصی دارد و وضعیت عملیات‌های پردازنده را مشخص می‌کند؛ مانند:

- نتیجه صفر بودن عملیات  
- وجود Carry  
- وجود Overflow

---

#### 4. رجیستر Instruction Pointer (RIP)

- یک رجیستر **۶۴ بیتی**

این رجیستر به **آدرس دستور بعدی که باید اجرا شود** در حافظه اشاره می‌کند.

این رجیستر در **Exploit Development** بسیار مهم است، زیرا اگر مهاجم بتواند مقدار آن را کنترل کند، می‌تواند مسیر اجرای برنامه را تغییر دهد.

---

### رجیسترهای عمومی و نحوه دسترسی به آن‌ها

رجیسترهای عمومی **۶۴ بیتی** هستند، اما دستورهای CPU می‌توانند به بخش‌های مختلف آن‌ها نیز دسترسی داشته باشند:

• دسترسی به کل رجیستر **۶۴ بیتی**  
• دسترسی به بخش **۳۲ بیتی پایین** رجیستر  
• دسترسی به بخش **۱۶ بیتی پایین** رجیستر  
• دسترسی به بخش **۸ بیتی پایین** رجیستر  

همچنین فقط **۴ رجیستر خاص** امکان دسترسی به **بخش بالایی (High Byte)** از قسمت ۱۶ بیتی پایین را دارند.
:::

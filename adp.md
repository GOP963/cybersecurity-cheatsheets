[```
comm -12 \
  <(grep -v '\$$' domainA.ntds | grep -v '31d6cfe0d16ae931b73c59d7e0c089c0' | cut -d':' -f4 | sort | uniq) \
  <(grep -v '\$$' domainB.ntds | grep -v '31d6cfe0d16ae931b73c59d7e0c089c0' | cut -d':' -f4 | sort | uniq) \
  > shared_hashes.txt
```





برای خروجی `impacket-secretsdump` که هر خط معمولاً این فرمت رو داره:

domain\username:RID:LMhash:NThash:::


چند حالت بسته به نیازت:

استخراج فقط NT hash (فیلد چهارم):

```bash
awk -F: '{print $4}' ntds1.txt
```

استخراج فقط LM hash (فیلد سوم):

```bash
awk -F: '{print $3}' ntds1.txt
```

اگر می‌خوای فقط خطوطی که واقعاً hash هستن رو فیلتر کنی (و خطوط Kerberos keys مثل `aes256-cts-hmac-sha1-96` یا cleartext رو حذف کنی)، با grep محدودش کن:

```bash
grep -E ':::$' ntds1.txt | awk -F: '{print $4}'
```

برای مقایسه دو دامپ با `diff`

چون ترتیب خروجی ممکنه فرق کنه، بهتره قبل از diff مرتب‌سازی کنی:

```bash
diff <(grep ':::$' ntds1.txt | awk -F: '{print $1":"$4}' | sort) \
     <(grep ':::$' ntds2.txt | awk -F: '{print $1":"$4}' | sort)
```

اینجا من `username:NThash` رو نگه داشتم که بفهمی کدوم اکانت hash‌ش تغییر کرده، نه فقط خود hash تنها.

اگه فقط می‌خوای ببینی چه hash‌هایی مشترک یا متفاوتن (بدون نام کاربر):

```bash
comm -3 <(awk -F: '/:::$/{print $4}' ntds1.txt | sort -u) \
        <(awk -F: '/:::$/{print $4}' ntds2.txt | sort -u)
```

بگو دقیقاً خروجی نهاییت چی باید باشه (فقط hash خام، یا `user:hash`، یا فقط اختلاف‌ها) تا دستور رو دقیق‌تر برات تنظیم کنم.




```
comm -12 \
  <(grep -v '\$:' domainA.ntds | cut -d':' -f4 | grep -v '31d6cfe0d16ae931b73c59d7e0c089c0' | sort -u) \
  <(grep -v '\$:' domainB.ntds | cut -d':' -f4 | grep -v '31d6cfe0d16ae931b73c59d7e0c089c0' | sort -u) \
  > shared_hashes.txt

```



```
join -t: -1 1 -2 1 \
  <(grep -F -f shared_hashes.txt domainA.ntds | awk -F: '{print $4":"$1}' | sort) \
  <(grep -F -f shared_hashes.txt domainB.ntds | awk -F: '{print $4":"$1}' | sort)

```







این یک شبکه داخلی خصوصی است (`172.16.0.0/12`)، پس فرض می‌کنم روی محدوده تحت مدیریت خودت اسکن می‌زنی.

محدوده‌ات کمی مبهم بود. دو حالت رایج:
- `172.20.0.1-25` (فقط ۲۵ هاست در یک ساب‌نت)
- `172.20.0.0/24` یا بزرگ‌تر

پاین فرض را روی `172.20.0.0/24` گذاشتم؛ در صورت نیاز اصلاحش کن.

## مرحله ۱: کشف هاست‌های زنده
ابتدا ببین چه هاست‌های بالا هستند تا اسکن پورت روی همه IPها هدر نود:

```bash
sudo nmap -sn 172.20.0.0/24 -oA host_discovery
```

- `-sn` فقط host discovery (بدون اسکن پورت)
- `-oA` خروجی را در سه فرمت (normal/grepable/xml) ذخیره می‌کند

استخراج لیست هاست‌های زنده:

```bash
grep "Up" host_discovery.gnmap | cut -d ' ' -f 2 > live_hosts.txt
```

## مرحله ۲: کشف پورت‌های باز
اسکن سریع روی هاست‌های زنده برای پیدا کردن پورت‌های باز:

```bash
sudo nmap -sS -p- --min-rate 2000 -iL live_hosts.txt -oA open_ports
```

- `-sS` اسکن SYN (سریع و کم‌سروصدا)
- `-p-` تمام ۶۵۳۵ پورت TCP
- `--min-rate 2000` سرعت ارسال پکت (در شبکه داخلی پایدار قابل افزایش است)

اگر زمان مهم است، فقط پورت‌های پرکاربرد:

```bash
sudo nmap -sS --top-ports 1000 -iL live_hosts.txt -oA top_ports
```

## مرحله ۳: شناسایی سرویس و نسخه + NSE
حالا فقط روی پورت‌های بازی که پیدا شد، بررسی دقیق سرویس و اسکریپت‌های آسیب‌پذیری:

```bash
sudo nmap -sV -sC --version-intensity 9 \
  -p <پورت‌های‌باز> -iL live_hosts.txt -oA service_scan
```

- `-sV` تشخیص نسخه سرویس
- `-sC` اجرای اسکریپت‌های پیش‌فرض NSE
- `--version-intensity 9` دقت بالا در تشخیص نسخه

## مرحله ۴: بررسی آسیب‌پذیری با NSE
برای ارزیابی آسیب‌پذیری، دسته `vuln`:

```bash
sudo nmap -sV --script vuln -p <پورت‌های‌باز> -iL live_hosts.txt -oA vuln_scan
```

اسکریپت‌های هدفمند بسته به سرویس کشف‌شده، مثلاً:

```bash
# SMB
sudo nmap -p 445 --script "smb-vuln-*" -iL live_hosts.txt

# HTTP
sudo nmap -p 80,443 --script "http-vuln-*,http-enum" -iL live_hosts.txt

# RDP
sudo nmap -p 3389 --script "rdp-vuln-*" -iL live_hosts.txt
```

## نکات عملی
- خروجی `-oA` را نگه دار؛ برای گزارش‌دهی و ورودی ابزارهای بعدی مثل اتصال نتایج به یک پایپ‌لاین کاربردی است.
- اسکریپت‌های `vuln` گاهی false positive دارند؛ یافته‌ها را دستی تأیید کن.
- اگر این شبکه AD است، بعد از کشف سرویس‌های SMB/LDAP/Kerberos می‌توان مرحله enumeration اختصاصی (مثل ورودی برای BloodHound) را جدا کرد.

بازه دقیق IP و اینکه TCP کافی است یا UDP هم لازم داری را بگو تا دستورها را دقیق‌تر تنظیم کنم.](<من ابتدا برای بررسی دقیق آسیب‌پذیری‌های شناخته‌شده (CVEها) مربوط به این نسخه خاص از SQL Server، یک جستجوی سریع انجام می‌دهم. 

توضیح: نسخه `15.0.2000.5` یا همان `15.00.2000` در واقع نسخه **RTM (نسخه اولیه و بدون آپدیت/سرویس‌پک/Cumulative Update)** از Microsoft SQL Server 2019 است که در سال ۲۰۱۹ منتشر شد. از آن زمان تاکنون، به‌روزرسانی‌های امنیتی متعددی برای این پایگاه داده ارائه شده است. اکنون جزئیات آسیب‌پذیری‌های ثبت‌شده برای این نسخه را جستجو می‌کنم.
بله — **این نسخه را باید آسیب‌پذیر و منسوخ از نظر امنیتی در نظر بگیرید**.

## نتیجه کوتاه
نسخه‌ای که گفتید:

```text
Microsoft SQL Server 2019 15.00.2000
```

عملاً همان **SQL Server 2019 RTM** است، یعنی **نسخه اولیه انتشار** (`15.0.2000.5`) و **بدون Cumulative Update**.  
این یعنی:

- بسیار قدیمی است
- بعد از انتشار اولیه، برای SQL Server 2019 چندین **به‌روزرسانی امنیتی** منتشر شده
- در نتیجه اگر هنوز روی `15.00.2000` هستید، **نباید آن را امن فرض کنید**

---

## شواهدی که قابل اتکا هستند

### 1) این بیلد همان RTM است
بیلد `15.0.2000.5` به‌عنوان **SQL Server 2019 RTM** شناخته می‌شود.

### 2) SQL Server 2019 در سال‌های بعد آسیب‌پذیری امنیتی دریافت کرده
حداقل موارد زیر برای خانواده SQL Server 2019 ثبت/رفع شده‌اند:

- **CVE-2021-1636**
- **CVE-2025-49759**  
  که در به‌روزرسانی‌های بعدی شاخه RTM هم به آن اشاره شده

این به‌تنهایی کافی است تا نتیجه بگیریم:
%3E **نسخه RTM اولیه (`15.00.2000`) قطعاً آخرین وصله‌های امنیتی را ندارد.**

---

## آیا می‌شود گفت «این نسخه دقیقاً به فلان CVE آسیب‌پذیر است»؟
با احتیاط باید گفت:

- **به‌صورت عملیاتی و امنیتی: بله، این نسخه ریسک‌دار است**
- اما برای اینکه بگوییم **دقیقاً کدام CVEها روی همین build خاص `15.0.2000.5` اثر دارند**، باید تطبیق دقیق با:
  - Security Advisory مایکروسافت
  - KBهای هر CU/GDR
  - ماتریس build-to-fix  
  انجام شود

یعنی الان **با اطمینان بالا** می‌شود گفت:
- این نسخه **outdated** است
- **security fixes زیادی را ندارد**
- بنابراین **vulnerable / unsupported-from-security-posture** محسوب می‌شود

اما اگر بخواهیم **لیست قطعی CVEهای مؤثر روی همین build** بدهیم، باید بررسی advisory-by-advisory انجام شود.

---

## ارزیابی عملی امنیتی

اگر این نسخه در محیط واقعی استفاده می‌شود، از دید امنیتی باید این‌طور برخورد کنید:

### سطح ریسک
- **متوسط تا بالا**، بسته به اینکه:
  - SQL Server روی اینترنت اکسپوز باشد یا نه
  - فقط داخلی باشد یا نه
  - آیا Domain-joined است
  - آیا xp_cmdshell / CLR / linked servers / SQL Agent / SSIS فعال هستند
  - آیا دسترسی کاربران محدود شده یا نه

### چرا خطرناک است؟
چون SQL Server قدیمی معمولاً در معرض این دسته ریسک‌هاست:
- **Remote Code Execution**
- **Privilege Escalation**
- **Information Disclosure**
- **Denial of Service**
- زنجیره شدن با ضعف‌های AD / Kerberos / Service Accounts

---

## توصیه حرفه‌ای
### اگر فقط می‌خواهید جواب بله/خیر:
**بله، این نسخه را آسیب‌پذیر فرض کنید و باید آپدیت شود.**

### اقدام پیشنهادی:
1. **نسخه دقیق کامل** را با این کوئری بگیرید:
   ```sql
   SELECT @@VERSION;
   GO
   ```

2. **ProductVersion / ProductLevel / Edition** را هم بگیرید:
   ```sql
   SELECT
       SERVERPROPERTY('ProductVersion') AS ProductVersion,
       SERVERPROPERTY('ProductLevel')   AS ProductLevel,
       SERVERPROPERTY('Edition')        AS Edition;
   GO
   ```

3. آن را با **آخرین CU/GDR برای SQL Server 2019** تطبیق دهید

4. اگر هنوز `RTM` یا CU خیلی قدیمی است:
   - به آخرین **CU** یا حداقل آخرین **Security Update** ارتقا دهید

---

## جمع‌بندی نهایی
نسخه:

```text
Microsoft SQL Server 2019 15.00.2000
```

**نسخه اولیه RTM** است و چون بعد از آن چندین وصله امنیتی برای SQL Server 2019 منتشر شده،  
**این نسخه را باید آسیب‌پذیر و نیازمند به‌روزرسانی در نظر گرفت**.

اگر بخواهید، در پیام بعدی می‌توانم یکی از این دو کار را انجام بدهم:

1. **لیست دقیق CVEهای مرتبط با SQL Server 2019 RTM** را برایتان جمع کنم  
2. یا **بگویم این build دقیقاً تا چه CU/KB باید ارتقا پیدا کند** تا از وضعیت ناامن خارج شود>)






---



بسیار عالی. کاملاً منطقی است؛ اسکن تمام ۶۵ هزار پورت در یک شبکه `/24` نه تنها زمان‌بر است، بلکه احتمال شناسایی شدن توسط سیستم‌های تشخیص نفوذ (IDS) را هم به شدت افزایش می‌دهد.

در حالت عادی، Nmap با سوئیچ `top-ports` به صورت پیش‌فرض ۱۰۰۰ پورت پرکاربرد را بررسی می‌کند.

اینجا یک روند بهینه و حرفه‌ای برای اسکن سریع و مرحله‌بندی شده آورده‌ام:

### مرحله ۱: اسکن سریع برای شناسایی میزبان‌های زنده و پورت‌های پرکاربرد
در این مرحله، Nmap ۱۰۰۰ پورت برتر (Top 1000) را بررسی می‌کند که ۹۰٪ سرویس‌های رایج در آن‌ها قرار دارند.

```bash
nmap -n -Pn --top-ports 1000 172.20.0.0/24 -oG top_ports.txt
```

*   **`--top-ports 1000`**: به جای اسکنِ کل پورت‌ها، فقط ۱۰۰۰ پورت پرکاربرد را چک می‌کند.
*   **خروجی**: نتیجه در فایل `top_ports.txt` ذخیره می‌شود.

---

### مرحله ۲: شناسایی دقیق سرویس و ورژن روی پورت‌های پیدا شده
حالا فقط روی پورت‌هایی که در مرحله قبل باز بوده‌اند، اسکنِ دقیق (Service Version Detection) را اجرا می‌کنیم. این کار باعث می‌شود زمانِ شما هدر نرود و مستقیماً روی "هدف‌های واقعی" متمرکز شوید.

برای این کار، می‌توانید از این دستور استفاده کنید تا فقط همان IPها و پورت‌هایی که واقعاً باز بودند هدف قرار بگیرند:

```bash
nmap -sV -sC -p <پورت‌های_شناسایی_شده> <آی‌پی_هدف>
```

---

### روش پیشنهادی برای اتوماسیون (سریع‌ترین حالت)

اگر می‌خواهید این فرآیند را به صورت حرفه‌ای و سریع انجام دهید، پیشنهاد می‌کنم از سوئیچ `-F` (Fast Mode) استفاده کنید. حالت Fast در Nmap، پورت‌ها را به ۱۰۰ پورت پرکاربرد محدود می‌کند که برای شناسایی اولیه عالی است:

```bash
# مرحله ۱: اسکن سریعِ ۱۰۰ پورت پرکاربرد در کل رنج
nmap -n -Pn -F 172.20.0.0/24 -oG fast_scan.txt

# مرحله ۲: استخراج خودکار و اسکن ورژن برای پورت‌های پیدا شده
# این دستور ساده تمام آی‌پی‌ها و پورت‌های باز را می‌گیرد و اسکنِ دقیق انجام می‌دهد
nmap -sV -sC -p $(grep "open" fast_scan.txt | awk -F'Ports: ' '{print $2}' | sed 's/\/open\/tcp\/\/\/ //g' | tr ',' ',' | sort -u | tr '\n' ',' | sed 's/,$//') <هدف>
```

### چند نکته طلایی برای این متد:
1.  **اگر تعداد پورت‌ها زیاد شد:** اگر در یک شبکه بزرگ هستید و خروجی مرحله ۱ زیاد است، می‌توانید به جای `1000` از `--top-ports 100` یا `--top-ports 200` استفاده کنید تا اسکن در چند ثانیه تمام شود.
2.  **استفاده از `-T4`:** برای افزایش سرعت در شبکه‌های داخلی و امن، می‌توانید سوئیچ `-T4` را اضافه کنید تا اسکن تهاجمی‌تر و سریع‌تر پیش برود.
3.  **دقت در سرویس‌ها:** چون هدف شما شناسایی ورژن است، حتماً `-sV` را در مرحله دوم حفظ کنید؛ چرا که گاهی اوقات سرویس‌های قدیمی (مثل همان SQL Server 2019 RTM شما) روی پورت‌های غیرمعمول اجرا می‌شوند، اما با سوئیچ `-sV` Nmap حتی اگر پورت استاندارد نباشد، سعی می‌کند با ارسال Fingerprint متوجه ورژن شود.



